import { React } from 'react'
import classes from './activeChat.module.css';

const activeChat = (props) => {
    return <div className={classes.activeChat}>
        activeChat
    </div>
}

export default activeChat;